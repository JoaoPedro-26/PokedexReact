import styled from "styled-components";

const StyledMain = styled.div`
    width: auto;
    margin-top: 50px;
    margin-left: 3%;
    display: grid;
    grid-gap: 44px 60px;
    grid-template-columns: repeat(auto-fit, max( 246px));
`;

export default StyledMain;